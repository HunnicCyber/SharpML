using System.IO;
using System.Reflection;

namespace SharpML
{
    class dropAlgoirthm
    {
        //public void dropAtoDisk(string drop_location)
        //{
        //    var stream = Assembly.GetManifestResourceStream("ml_model.exe");
        //    var fileStream = File.Create(drop_location + @"\model.exe");
        //    stream.Seek(0, SeekOrigin.Begin);
        //    stream.CopyTo(fileStream);
        //    fileStream.Close();
        //}
    }
}
